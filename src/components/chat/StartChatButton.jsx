import React from 'react';

export default function StartChatButton({ proofOwnerEmail, proofId }) {
  // Wallet functionality has been removed, so this button does nothing for now.
  // In the future, it could link to a messaging system based on user emails/IDs.
  return null;
}